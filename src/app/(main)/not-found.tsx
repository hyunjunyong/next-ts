import { notFound } from "next/navigation";

export default async function Detail() {
  return <div>123</div>;
  //   if (result == null) {
  //     return notFound()
  //   } else {
  //     return (상세페이지)
  //   }
}
